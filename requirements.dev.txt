twine
sphinx
sphinx-rtd-theme
m2r2

# Test / lint / typecheck — keep in sync with [project.optional-dependencies] dev
# in pyproject.toml (`pip install -e ".[dev]"` should yield the same set).
pytest>=8.0
pytest-cov>=5.0
moto[s3]>=5.0
mypy>=1.10
ruff>=0.5
build>=1.0
